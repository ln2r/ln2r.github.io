import { Inter, Plus_Jakarta_Sans, Quicksand, Redacted } from "next/font/google";

export const redacted = Redacted({
  subsets: ['latin'],
  weight: '400',
})

export const inter = Inter({
  subsets: ['latin'],
  weight: '400',
})

export const inter_light = Inter({
  subsets: ['latin'],
  // weight: '200',
})

export const jakarta_sans = Plus_Jakarta_Sans({
  subsets: ['latin',]
})
